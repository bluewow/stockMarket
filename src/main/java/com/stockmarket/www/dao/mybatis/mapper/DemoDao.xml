<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper namespace의 값은 mapping 시키려는 Dao Interface의 namespace와 정확하게 일치해야 한다. -->
<mapper namespace="com.stockmarket.www.dao.DemoDao">
	<!-- 
		mapping된 select 구문
		insert 이외에도 update, insert, delete 구문이 있다.
		
		id는 mapping 시키려는 함수의 이름과 일치해야한다.
		
		resultType : 이 구문에 의해 리턴되는 기대타입의 패키지 경로를 포함한 전체 클래스명이나 별칭. 
		collection인 경우 collection 타입 자체가 아닌 collection 이 포함된 타입이 될 수 있다.
		
		select 이외에 구문에 전달될 것이 있다면 parameterType을 사용한다.
		parameterType : 구문에 전달될 파라미터의 패키지 경로를 포함한 전체 클래스명이나 별칭
		
		그외 mybatis에 관한 정보 : https://mybatis.org/mybatis-3/ko/sqlmap-xml.html
	 -->
	<select id="getMemberList" resultType="com.stockmarket.www.entity.Member">
		select * from Member
	</select>
</mapper>



