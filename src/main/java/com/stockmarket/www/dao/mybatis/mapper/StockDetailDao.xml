<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockmarket.www.dao.StockDetailDao">
	<select id="get" resultType="com.stockmarket.www.entity.StockDetail">
		SELECT * FROM StockDetail WHERE codeNum=#{codeNum} order by recordDate desc
	</select>
			
	<insert id="insert" parameterType="com.stockmarket.www.entity.StockDetail">
		INSERT INTO StockDetail (codeNum, recordDate, closePrice, tradeVolume, foreign, institution, indivisual) 
			+ VALUES (#{itemcode},#{bizdate},#{close_val},#{acc_quant},#{frgn_pure_buy_quant},#{organ_pure_buy_quant},#{indi_pure_buy_quant})
	</insert>
	
	<insert id="insertDaily" parameterType="com.stockmarket.www.entity.StockDetail">
		<!-- TODO -->
	</insert>
	
	<delete id="deletePreDate">
		DELETE StockDetail WHERE recordDate &lt;= 20151231
	</delete>
	
	<delete id="delete">
		<!-- TODO -->
	</delete>
</mapper>